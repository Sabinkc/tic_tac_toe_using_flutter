import 'package:flutter/material.dart';

class GameScreenWithFriend extends StatelessWidget {
  const GameScreenWithFriend({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
